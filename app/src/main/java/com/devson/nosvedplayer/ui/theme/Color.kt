package com.devson.nosvedplayer.ui.theme

import androidx.compose.ui.graphics.Color

//  Nosved Blue Palette 
// Google-style: periwinkle-blue bg + white cards + Google-blue accent

// Primary - Google Blue
val NosvedBlue80        = Color(0xFFAEC6FF)   // Light tint  (dark theme primary)
val NosvedBlue40        = Color(0xFF1A73E8)   // Saturated   (light theme primary)
val NosvedBlueDeep      = Color(0xFF0D47A1)   // Deep blue   (dark primaryContainer text)

// Secondary - Indigo / periwinkle
val NosvedIndigo80      = Color(0xFFC5CAE9)   // Light tint  (dark theme secondary)
val NosvedIndigo40      = Color(0xFF3949AB)   // Medium      (light theme secondary)

// Tertiary - Google Green accent
val NosvedGreen80       = Color(0xFFB9F6CA)   // Light       (dark theme tertiary)
val NosvedGreen40       = Color(0xFF1E8E3E)   // Medium      (light theme tertiary)

//  Light Theme Surfaces (Google Play / Material 3 style) 
val NosvedLightBg       = Color(0xFFEEF1FB)   // Soft periwinkle-blue background ★
val NosvedLightSurface  = Color(0xFFFFFFFF)   // Pure white cards / dialogs
val NosvedLightSurface2 = Color(0xFFF3F6FF)   // Slightly tinted surface container
val NosvedLightOutline  = Color(0xFFC5C9E0)   // Border / divider
val NosvedLightOnSurf   = Color(0xFF1A1C22)   // Near-black primary text
val NosvedLightSubtext  = Color(0xFF44474F)   // Secondary / hint text
val NosvedLightPrimCont = Color(0xFFDAE2FF)   // Primary container (chips, badges)
val NosvedLightSecCont  = Color(0xFFE0E5FF)   // Secondary container (icon bgs)

//  Dark Theme Surfaces 
val NosvedDarkBg        = Color(0xFF0E1117)   // Background
val NosvedDarkSurface   = Color(0xFF161B27)   // Card / surface level 1
val NosvedDarkSurface2  = Color(0xFF1E2537)   // Card / surface level 2 (elevated)
val NosvedDarkSurface3  = Color(0xFF222C3F)   // Highest surface container
val NosvedDarkOutline   = Color(0xFF3A4660)   // Border / outline
val NosvedDarkOnSurface = Color(0xFFE2E5F0)   // Primary text on dark
val NosvedDarkSubtext   = Color(0xFF8D97B2)   // Secondary / hint text on dark

//  Player accent & status 
val NosvedSeekBarTint   = Color(0xFF1A73E8)   // Progress / scrubber tint  (matches primary)
val NosvedErrorRed      = Color(0xFFBA1A1A)   // Error / destructive action